<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Happy Birthday Fatiha!</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background: linear-gradient(135deg, #ffeef8 0%, #e8d5f2 50%, #ffc4e8 100%);
            background-size: 400% 400%;
            animation: gradient-shift 15s ease infinite;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            padding: 20px;
        }

        @keyframes gradient-shift {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .container {
            text-align: center;
        }

        .card {
            width: 450px;
            max-width: 100%;
            background: white;
            border-radius: 35px;
            box-shadow: 0 15px 60px rgba(199, 21, 133, 0.2);
            position: relative;
            overflow: hidden;
            transition: all 0.5s cubic-bezier(0.68, -0.55, 0.265, 1.55);
            animation: card-entrance 1s ease-out;
        }

        @keyframes card-entrance {
            from {
                opacity: 0;
                transform: translateY(50px) scale(0.9);
            }
            to {
                opacity: 1;
                transform: translateY(0) scale(1);
            }
        }

        .card:hover {
            transform: translateY(-10px) scale(1.02);
            box-shadow: 0 25px 80px rgba(199, 21, 133, 0.35);
        }

        .card-header {
            height: 220px;
            background: linear-gradient(135deg, #ffc4e8 0%, #dab8f5 50%, #f5a7e0 100%);
            background-size: 200% 200%;
            animation: header-gradient 8s ease infinite;
            position: relative;
            overflow: hidden;
        }

        @keyframes header-gradient {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .floating-elements {
            position: absolute;
            width: 100%;
            height: 100%;
        }

        .float-item {
            position: absolute;
            opacity: 0.4;
            animation: gentle-float 8s infinite ease-in-out;
            filter: drop-shadow(2px 2px 4px rgba(0,0,0,0.1));
        }

        .float-item:nth-child(1) { left: 10%; top: 20%; font-size: 28px; animation-delay: 0s; }
        .float-item:nth-child(2) { left: 70%; top: 15%; font-size: 24px; animation-delay: 1s; }
        .float-item:nth-child(3) { left: 85%; top: 65%; font-size: 26px; animation-delay: 2s; }
        .float-item:nth-child(4) { left: 15%; top: 75%; font-size: 22px; animation-delay: 1.5s; }
        .float-item:nth-child(5) { left: 45%; top: 40%; font-size: 20px; animation-delay: 2.5s; }
        .float-item:nth-child(6) { left: 60%; top: 80%; font-size: 24px; animation-delay: 3s; }

        @keyframes gentle-float {
            0%, 100% { 
                transform: translateY(0px) translateX(0px) rotate(0deg);
                opacity: 0.4;
            }
            25% { 
                transform: translateY(-15px) translateX(10px) rotate(5deg);
                opacity: 0.6;
            }
            50% { 
                transform: translateY(-25px) translateX(-5px) rotate(-3deg);
                opacity: 0.5;
            }
            75% { 
                transform: translateY(-10px) translateX(-10px) rotate(7deg);
                opacity: 0.7;
            }
        }

        .sparkle-layer {
            position: absolute;
            width: 100%;
            height: 100%;
            pointer-events: none;
        }

        .sparkle {
            position: absolute;
            width: 4px;
            height: 4px;
            background: white;
            border-radius: 50%;
            animation: sparkle-twinkle 3s infinite;
            box-shadow: 0 0 10px rgba(255, 255, 255, 0.8);
        }

        .sparkle:nth-child(1) { left: 20%; top: 30%; animation-delay: 0s; }
        .sparkle:nth-child(2) { left: 80%; top: 25%; animation-delay: 0.5s; }
        .sparkle:nth-child(3) { left: 35%; top: 60%; animation-delay: 1s; }
        .sparkle:nth-child(4) { left: 65%; top: 70%; animation-delay: 1.5s; }
        .sparkle:nth-child(5) { left: 50%; top: 45%; animation-delay: 2s; }
        .sparkle:nth-child(6) { left: 90%; top: 50%; animation-delay: 2.5s; }

        @keyframes sparkle-twinkle {
            0%, 100% { 
                opacity: 0; 
                transform: scale(0) rotate(0deg);
            }
            50% { 
                opacity: 1; 
                transform: scale(1.5) rotate(180deg);
            }
        }

        .card-body {
            padding: 45px 35px 40px;
            position: relative;
        }

        .cake-container {
            margin: 35px 0;
            cursor: pointer;
            transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
            display: inline-block;
        }

        .cake-container:hover {
            transform: scale(1.2) rotate(5deg);
        }

        .cake-container:active {
            transform: scale(1.1) rotate(-5deg);
        }

        .cake {
            font-size: 100px;
            display: inline-block;
            animation: cake-bounce 3s infinite ease-in-out;
            filter: drop-shadow(0 5px 15px rgba(244, 114, 182, 0.4));
        }

        @keyframes cake-bounce {
            0%, 100% { transform: translateY(0) rotate(0deg); }
            25% { transform: translateY(-10px) rotate(-2deg); }
            50% { transform: translateY(-15px) rotate(0deg); }
            75% { transform: translateY(-10px) rotate(2deg); }
        }

        h1 {
            font-size: 46px;
            background: linear-gradient(135deg, #e879f9, #c084fc, #f472b6);
            background-size: 200% 200%;
            animation: text-gradient 5s ease infinite;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 15px;
            font-weight: 700;
            letter-spacing: 1px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
        }

        @keyframes text-gradient {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .name {
            font-size: 58px;
            color: #c026d3;
            margin-bottom: 30px;
            font-weight: 800;
            animation: name-glow 3s ease-in-out infinite;
            text-shadow: 0 0 20px rgba(192, 38, 211, 0.3);
        }

        @keyframes name-glow {
            0%, 100% { 
                text-shadow: 0 0 20px rgba(192, 38, 211, 0.3),
                             0 0 30px rgba(192, 38, 211, 0.2);
            }
            50% { 
                text-shadow: 0 0 30px rgba(192, 38, 211, 0.6),
                             0 0 50px rgba(192, 38, 211, 0.4);
            }
        }

        .message {
            font-size: 20px;
            color: #666;
            line-height: 1.8;
            margin-bottom: 35px;
            animation: fade-in-up 1.5s ease-out 0.5s both;
        }

        @keyframes fade-in-up {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .intro-text {
            font-size: 17px;
            color: #888;
            line-height: 1.9;
            margin-bottom: 30px;
            padding: 20px;
            background: linear-gradient(135deg, #fdf4ff 0%, #fce7f3 100%);
            border-radius: 20px;
            border-left: 4px solid #f472b6;
            animation: fade-in-up 1.5s ease-out 0.8s both;
        }

        .button-container {
            display: flex;
            gap: 15px;
            justify-content: center;
            flex-wrap: wrap;
            animation: fade-in-up 1.5s ease-out 1.1s both;
        }

        .btn {
            padding: 14px 32px;
            border: none;
            border-radius: 30px;
            font-size: 17px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
            outline: none;
            position: relative;
            overflow: hidden;
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.3);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }

        .btn:active::before {
            width: 300px;
            height: 300px;
        }

        .btn-primary {
            background: linear-gradient(135deg, #f472b6, #c084fc, #e879f9);
            background-size: 200% 200%;
            animation: button-gradient 3s ease infinite;
            color: white;
            box-shadow: 0 6px 20px rgba(244, 114, 182, 0.4);
        }

        @keyframes button-gradient {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .btn-primary:hover {
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 10px 30px rgba(244, 114, 182, 0.6);
        }

        .btn-secondary {
            background: white;
            color: #c026d3;
            border: 3px solid #f472b6;
            box-shadow: 0 4px 15px rgba(192, 38, 211, 0.2);
        }

        .btn-secondary:hover {
            background: linear-gradient(135deg, #fdf4ff, #fce7f3);
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 8px 25px rgba(192, 38, 211, 0.3);
        }

        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.6);
            backdrop-filter: blur(5px);
            justify-content: center;
            align-items: center;
            z-index: 1000;
            animation: modal-fade-in 0.4s ease;
            padding: 20px;
        }

        @keyframes modal-fade-in {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .modal-content {
            background: linear-gradient(135deg, #ffffff 0%, #fdf4ff 100%);
            padding: 50px 45px;
            border-radius: 30px;
            max-width: 520px;
            width: 100%;
            text-align: center;
            animation: modal-slide-up 0.5s cubic-bezier(0.68, -0.55, 0.265, 1.55);
            box-shadow: 0 30px 80px rgba(0, 0, 0, 0.4);
            border: 3px solid rgba(244, 114, 182, 0.3);
            position: relative;
            overflow: hidden;
            max-height: 90vh;
            overflow-y: auto;
        }

        .modal-content::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(45deg, 
                transparent, 
                rgba(244, 114, 182, 0.1), 
                transparent);
            animation: modal-shine 3s infinite;
        }

        @keyframes modal-shine {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        @keyframes modal-slide-up {
            from { 
                transform: translateY(100px) scale(0.8);
                opacity: 0;
            }
            to { 
                transform: translateY(0) scale(1);
                opacity: 1;
            }
        }

        .modal-content h2 {
            color: #c026d3;
            margin-bottom: 30px;
            font-size: 36px;
            font-weight: 700;
            position: relative;
            z-index: 1;
            animation: name-glow 3s ease-in-out infinite;
        }

        .modal-content p {
            color: #555;
            line-height: 2;
            font-size: 19px;
            margin-bottom: 30px;
            position: relative;
            z-index: 1;
        }

        .modal-content strong {
            color: #c026d3;
            font-weight: 700;
        }

        .heart-divider {
            margin: 25px 0;
            font-size: 24px;
            animation: heart-beat 1.5s ease-in-out infinite;
        }

        @keyframes heart-beat {
            0%, 100% { transform: scale(1); }
            25% { transform: scale(1.1); }
            50% { transform: scale(1); }
            75% { transform: scale(1.15); }
        }

        .close-btn {
            background: linear-gradient(135deg, #f472b6, #c084fc);
            color: white;
            border: none;
            padding: 14px 35px;
            border-radius: 30px;
            cursor: pointer;
            font-size: 17px;
            font-weight: 600;
            transition: all 0.3s ease;
            box-shadow: 0 6px 20px rgba(244, 114, 182, 0.4);
            position: relative;
            z-index: 1;
        }

        .close-btn:hover {
            transform: scale(1.1) translateY(-2px);
            box-shadow: 0 10px 30px rgba(244, 114, 182, 0.6);
        }

        .confetti-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 999;
        }

        .confetti {
            position: absolute;
            width: 12px;
            height: 12px;
            animation: confetti-fall 4s linear forwards;
        }

        @keyframes confetti-fall {
            to {
                transform: translateY(100vh) rotate(720deg);
                opacity: 0;
            }
        }

        .signature {
            margin-top: 30px;
            font-size: 18px;
            color: #c084fc;
            font-style: italic;
            font-weight: 500;
            animation: fade-in-up 1.5s ease-out 1.4s both;
        }

        .hearts-trail {
            position: fixed;
            pointer-events: none;
            font-size: 20px;
            animation: hearts-float-up 2s ease-out forwards;
            z-index: 9999;
        }

        @keyframes hearts-float-up {
            0% {
                opacity: 1;
                transform: translateY(0) scale(1) rotate(0deg);
            }
            100% {
                opacity: 0;
                transform: translateY(-100px) scale(1.5) rotate(20deg);
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="card">
            <div class="card-header">
                <div class="floating-elements">
                    <div class="float-item">💝</div>
                    <div class="float-item">✨</div>
                    <div class="float-item">🌸</div>
                    <div class="float-item">💕</div>
                    <div class="float-item">🎀</div>
                    <div class="float-item">🌟</div>
                </div>
                <div class="sparkle-layer">
                    <div class="sparkle"></div>
                    <div class="sparkle"></div>
                    <div class="sparkle"></div>
                    <div class="sparkle"></div>
                    <div class="sparkle"></div>
                    <div class="sparkle"></div>
                </div>
            </div>
            
            <div class="card-body">
                <h1>Happy Birthday</h1>
                <div class="name">Fatiha!</div>
                
                <div class="cake-container" onclick="celebrate()">
                    <div class="cake">🎂</div>
                </div>
                
                <div class="message">
                    To the best friend I've ever had<br>
                    and will ever have 💕
                </div>

                <div class="intro-text">
                    You mean the absolute world to me. Every moment we've shared, every laugh, every tear, every late-night conversation has shaped who I am today. I don't know what I'd do without you. 🌸
                </div>
                
                <div class="button-container">
                    <button class="btn btn-primary" onclick="showMessage()">Read My Heart 💖</button>
                    <button class="btn btn-secondary" onclick="celebrate()">Let's Celebrate! 🎉</button>
                </div>
                
                <div class="signature">Forever yours, Your Bestie 🌺</div>
            </div>
        </div>
    </div>

    <div class="modal" id="messageModal">
        <div class="modal-content">
            <h2>Dear Fatiha 💖</h2>
            <p>
                <strong>You are the best friend I've ever had and will ever have.</strong> These aren't just words - they're the absolute truth from the deepest part of my heart.
            </p>
            <div class="heart-divider">💕</div>
            <p>
                I can't imagine my life without you in it. You've been there through every high and low, celebrating my wins and lifting me up when I fall. You understand me in ways no one else ever has or ever will.
            </p>
            <div class="heart-divider">💝</div>
            <p>
                Thank you for being my rock, my confidant, my partner in crime, and my biggest cheerleader. Thank you for accepting me completely - with all my flaws, my quirks, my messy moments.
            </p>
            <div class="heart-divider">🌟</div>
            <p>
                You've made my life infinitely brighter just by being you. The memories we've created together are treasures I'll hold onto forever. From the silly moments that made us laugh until we cried, to the deep conversations that lasted until sunrise.
            </p>
            <div class="heart-divider">✨</div>
            <p>
                On your special day, I want you to know how incredibly grateful I am for you. You deserve all the happiness, love, and success in the world. May this year bring you everything your heart desires and more.
            </p>
            <div class="heart-divider">💖</div>
            <p>
                <strong>Happy Birthday to my forever person!</strong> I love you more than words could ever express. Here's to countless more years of friendship, adventures, and making beautiful memories together! 🎉
            </p>
            <div class="heart-divider">🌸</div>
            <p style="font-style: italic; color: #c026d3; font-size: 21px;">
                You're not just my best friend - you're my chosen family. Today and always, I celebrate you! 💕
            </p>
            <button class="close-btn" onclick="closeMessage()">Thank You! This Means Everything! 💕</button>
        </div>
    </div>

    <div class="confetti-container" id="confettiContainer"></div>

    <script>
        function showMessage() {
            document.getElementById('messageModal').style.display = 'flex';
            celebrate();
        }

        function closeMessage() {
            document.getElementById('messageModal').style.display = 'none';
        }

        function celebrate() {
            const container = document.getElementById('confettiContainer');
            const colors = ['#f472b6', '#c084fc', '#e879f9', '#fbbf24', '#60a5fa', '#fb923c', '#a78bfa'];
            const shapes = ['circle', 'square'];
            
            for (let i = 0; i < 80; i++) {
                setTimeout(() => {
                    const confetti = document.createElement('div');
                    confetti.className = 'confetti';
                    confetti.style.left = Math.random() * 100 + '%';
                    confetti.style.background = colors[Math.floor(Math.random() * colors.length)];
                    confetti.style.animationDelay = Math.random() * 0.3 + 's';
                    confetti.style.animationDuration = (Math.random() * 2 + 3) + 's';
                    
                    const size = Math.random() * 8 + 8;
                    confetti.style.width = size + 'px';
                    confetti.style.height = size + 'px';
                    
                    if (shapes[Math.floor(Math.random() * shapes.length)] === 'square') {
                        confetti.style.borderRadius = '2px';
                    }
                    
                    container.appendChild(confetti);
                    
                    setTimeout(() => confetti.remove(), 4000);
                }, i * 20);
            }
        }

        // Close modal when clicking outside
        document.getElementById('messageModal').addEventListener('click', function(e) {
            if (e.target === this) {
                closeMessage();
            }
        });

        // Hearts trail effect on mouse move
        let heartTimeout;
        document.addEventListener('mousemove', function(e) {
            if (heartTimeout) return;
            
            heartTimeout = setTimeout(() => {
                const hearts = ['💕', '💖', '💗', '💝'];
                const heart = document.createElement('div');
                heart.className = 'hearts-trail';
                heart.textContent = hearts[Math.floor(Math.random() * hearts.length)];
                heart.style.left = e.pageX + 'px';
                heart.style.top = e.pageY + 'px';
                document.body.appendChild(heart);
                
                setTimeout(() => heart.remove(), 2000);
                heartTimeout = null;
            }, 100);
        });

        // Auto-celebrate on load
        window.addEventListener('load', () => {
            setTimeout(celebrate, 500);
        });
    </script>
</body>
</html>
